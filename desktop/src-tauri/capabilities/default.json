{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core IPC + menu-navigation events for the main window, including once it has navigated from the bundled splash to the local Node server.",
  "windows": ["main"],
  "permissions": ["core:default", "core:event:default"],
  "remote": {
    "urls": ["http://localhost:*"]
  }
}
